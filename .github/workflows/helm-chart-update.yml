name: ui tests

on:
  push:
    paths:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Helm
        run: |
          helm help
